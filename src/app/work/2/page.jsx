import ScrollSlider from "@/components/ScrollSlider/ScrollSlider";
import React from "react";

const page = () => {
  return (
    <div>
      <ScrollSlider />
    </div>
  );
};

export default page;
